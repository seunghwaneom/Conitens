
> @conitens/dashboard@0.1.0 test D:\Google\.Conitens\packages\dashboard
> node --experimental-strip-types --test tests/*.test.mjs "office-preview-shell.test.mjs"

✔ agent office profiles map orchestrator-like ids to the ops control persona (1.319ms)
✔ agent office profiles map implementer-like ids to the implementation persona (1.6102ms)
✔ agent office profiles map validator-like ids to the validation persona (0.2203ms)
✔ dashboard office canonical roles align with command-center roles (0.9861ms)
✔ dashboard office home rooms align with floorplate room ids (0.695ms)
✔ agent attention ordering prioritizes review and blocked work (2.1364ms)
✔ resolveDashboardData falls back to demo data when live store is empty (1.7918ms)
✔ resolveDashboardData keeps demo tasks and agents when only events exist (0.2765ms)
✔ derived dashboard helpers preserve current overview metrics and ordering (0.8823ms)
✔ tab badges preserve active task load and timeline volume (0.2436ms)
✔ priority task helper orders blocked and review work ahead of active tasks (0.3208ms)
✔ runtime ledger keeps the right rail focused on the three highest-signal items (0.2727ms)
✔ overview actions combine priority tasks with approval and handoff signals (14.5308ms)
✔ overview actions return an empty list when there are no urgent tasks or signals (0.2325ms)
✔ overview actions can be composed from signals only (0.336ms)
✔ overview actions honor the task budget when signals are present (0.4158ms)
✔ office snapshot maps room occupancy and handoff routes from current dashboard data (0.8879ms)
✔ event store reduces task lifecycle events into task state (2.0979ms)
✔ event store reduces agent lifecycle events into agent state (0.3278ms)
✔ parseRunsResponse validates the BE-1a run list shape (2.4749ms)
✔ parseOperatorSummaryResponse validates the overview summary shape (0.4061ms)
✔ parseOperatorSummaryResponse accepts optional evidence and doctor blocks (1.2375ms)
✔ parseOperatorEvidenceSummaryResponse and parseOperatorDoctorEvidenceResponse validate standalone shapes (0.2403ms)
✔ parseOperatorRuntimeRosterResponse validates runtime availability and checkpoint evidence (0.28ms)
✔ parseOperatorWakeReadinessResponse validates read-only wake candidates (1.1ms)
✔ parseOperatorInboxResponse validates the inbox summary shape (0.3221ms)
✔ parseOperatorAgentsResponse validates the agents projection shape (0.3842ms)
✔ parseOperatorTasksResponse and parseOperatorTaskDetailResponse validate the owned task shape (1.7195ms)
✔ parseOperatorTaskDeleteResponse validates the delete payload shape (0.2863ms)
✔ parseOperatorTaskReconcilePreviewResponse validates task preview shape (0.3327ms)
✔ parseOperatorWorkspacesResponse and parseOperatorWorkspaceDetailResponse validate the owned workspace shape (0.3744ms)
✔ workspace quick archive stays gated on rationale and mutation bodies only carry archive note when archiving (1.47ms)
✔ task quick status body is based on persisted task state, not unsaved editor draft (0.4264ms)
✔ task filter storage helpers persist current filter state and presets (0.4434ms)
✔ parseRunDetailResponse validates the BE-1a detail shape (0.3381ms)
✔ forward route round-trips between hash and route object (1.0219ms)
✔ forward view models stay compact and UI-shaped (0.4084ms)
✔ parseReplayResponse validates the replay timeline shape (0.4026ms)
✔ parseStateDocsResponse validates projected markdown payloads (0.2029ms)
✔ parseContextLatestResponse preserves runtime and repo digests separately (0.1681ms)
✔ parseRoomTimelineResponse validates room timeline payloads (0.2258ms)
✔ parseApprovalsResponse validates approval list payloads (0.3834ms)
✔ parseApprovalDetailResponse and parseApprovalResumeResponse validate mutation payloads (0.2434ms)
✔ parseStreamSnapshot validates live snapshot payloads (0.2165ms)
✔ parseForwardEventStreamChunk parses snapshot and heartbeat frames (0.3141ms)
✔ pickInitialApprovalId prefers pending approvals first (0.1799ms)
✔ pickNextApprovalId resets stale selection when the current id is absent (0.2041ms)
✔ pickNextRoomId preserves the current room when it still exists (0.1875ms)
✔ pickNextRoomId falls back to the first room when the current room is stale (0.1169ms)
✔ FE-7 insight helpers merge run and room insights and preserve summaries (14.9532ms)
✔ deriveForwardGraphModel builds graph nodes and edges from detail and replay (1.4868ms)
✔ deriveForwardGraphModel falls back to summary when structure is too sparse (0.2514ms)
✔ hybrid room-to-team mapping matches the agreed floorplate model (1.968ms)
✔ office presence model projects residents with team labels from the stage schema (18.7691ms)
✔ office selection falls back to the first visible resident in the chosen room (0.6067ms)
✔ office presence model uses overflow counts instead of rendering extra room clutter (0.3344ms)
✔ fixture registry covers every room and corridor fixture kind in the floorplate schema (0.3895ms)
✔ fixture registry resolves pixel-art PNG assets instead of SVG sheets (0.534ms)
✔ dashboard office profiles inherit canonical default rooms and sprite sheet paths from command-center (0.2683ms)
✔ every floorplate room has a team brief and the commons carries an ambient focal cluster (0.2914ms)
✔ impl office is densified with workbench support and ambient fixtures (0.3032ms)
✔ specialist wing rooms keep lean identities with restrained polish (0.7098ms)
✔ office presence model emits at most two visible task nodes per room with priority ordering (1.0285ms)
✔ avatar registry resolves every pose/facing variant used by the stage without dead inline sprite helpers (0.9421ms)
✔ office preview shell makes focused workbench dominant (2.3316ms)
✖ office preview shell gives Floor Overview a map plus inspector command center (4.0046ms)
✔ forward shell nav keeps shortened one-row labels before mobile (1.1401ms)
✔ office rail view caps visible rows and reports hidden counts (1.4547ms)
✔ office rail view reports zero hidden counts when data fits the rail (0.2059ms)
✔ office focus strip favors resident detail when a resident is selected (1.0488ms)
✔ office focus strip falls back to room summary when no resident is selected (0.387ms)
✔ agent station model reuses authored room slots (3.3984ms)
✔ agent visual roles map named Conitens agents to sprite roles (0.2769ms)
✔ agent visual states prioritize blocked, review, active, and assigned work (0.4011ms)
✔ owner review state uses the curated generated sprite frame (0.5647ms)
✔ handoff targets make sentinel read as a review receiver (0.2763ms)
✔ agent station matching prefers role-specific slots in the resident room (0.3865ms)
✔ activity cues distinguish live agent states (0.3258ms)
✔ operator focus map only keeps live, reviewing, and handoff agents on the floor (0.3344ms)
✔ spatial lens asset registry covers the required manifest groups (4.1218ms)
✔ spatial lens asset manifest stays local and structurally valid (0.9048ms)
✔ spatial lens registry references existing local asset files (3.446ms)
✔ spatial lens registry resolves existing local assets and placeholders (0.3771ms)
✔ spatial lens manual-import root documents the future public asset slot (0.1416ms)
✔ spatial lens floor viewport model maps office rooms into static geometry (13.0831ms)
✔ spatial lens floor viewport exposes visible handoff and blocked lane overlays (0.838ms)
✔ spatial lens floor viewport model chooses stable floor surfaces (0.1872ms)
✔ spatial lens floor viewport fixtures resolve through the asset registry (0.4961ms)
✔ spatial lens floor viewport falls back to the first room when selection is stale (0.2634ms)
✔ spatial lens building layout creates one connected floorplate (1.2979ms)
✔ spatial lens corridor graph keeps overview corridor narrow and purposeful (0.406ms)
✔ spatial lens room placements provide door-aligned corridor connections (1.2766ms)
✔ spatial lens handoff route uses doors, corridor, hub, and inbox target (0.3298ms)
✔ generated Spatial Lens sprite manifest is local and bounded (3.4344ms)
✔ generated Spatial Lens manifest includes implementation-critical sprites (0.2706ms)
✔ generated Spatial Lens sprites back required PixelProp kinds (0.3231ms)
✔ generated Spatial Lens room backdrops are local and focused-use bounded (0.523ms)
✔ generated Spatial Lens renderers expose sprite-gen curation hooks (1.277ms)
✔ spatial lens pixel grammar uses integer scale and hard shadow anchors (0.9676ms)
✔ spatial lens pixel grammar snaps props to the room tile field (0.6619ms)
✔ spatial lens pixel grammar y-sorts props deterministically (8.9493ms)
✔ spatial lens focused camera defaults to ops control and enlarges the floor (0.7854ms)
✔ spatial lens focused camera biases toward a connected handoff target (0.1931ms)
✔ spatial lens floor overview camera keeps the full topology at 1x (0.1678ms)
✔ focused handoff workbench model surfaces the active blocker chain (2.4139ms)
✔ focused handoff workbench model does not invent a blocked owner gate (0.3917ms)
✔ focused handoff workbench blocked age starts from the block-opening event (0.2968ms)
✔ focused handoff workbench model emits empty labels and correct edges without events (0.2994ms)
✔ getAgentWorkState returns REVIEW for a handoff target and BLOCKED for a blocked-task assignee (0.1443ms)
✔ agent work state vocabulary is shared between workbench and sidebar rail (0.4009ms)
✔ spatial lens handoff packet is an in-world object with a floor slot (0.4852ms)
✔ spatial lens handoff route uses corridor guide tiles for storytelling (0.4087ms)
✔ spatial lens floor viewport no longer mounts focused corridor continuity (0.2352ms)
✔ spatial lens focused mode renders the workbench instead of a floor viewport (0.3476ms)
✔ spatial lens focused mode exposes one active handoff workbench (1.1116ms)
✔ spatial lens handoff relation keeps route motion in overview and task identity in the workbench (0.5705ms)
✔ spatial lens focused mode no longer exports offscreen agent rail (0.2871ms)
✔ spatial lens CSS uses only integer scale transforms (0.4024ms)
✔ spatial lens pixel tokens expose the limited status palette (1.1187ms)
✔ spatial lens status tone normalization keeps UI states predictable (0.1421ms)
✔ spatial lens room dressing templates cover every office room (1.9373ms)
✔ spatial lens room dressing satisfies room-level density requirements (9.1245ms)
✔ spatial lens room dressing implements every required prop kind (0.461ms)
✔ spatial lens room depth layer gives rooms authored pixel depth (0.6817ms)
✔ spatial lens room kit layer uses generated signature sprites (0.695ms)
✔ spatial lens generated room backdrops are focused-only room material (0.6767ms)
✔ spatial lens room dressing anchors operational affordances to props (0.2412ms)
✔ spatial lens overview declutter rules are present in the CSS (0.3476ms)
✔ getEventFamily extracts prefix before first dot (1.2585ms)
✔ getEventFamily returns the full string when there is no dot (0.1638ms)
✔ getEventFamily handles multi-segment types (0.1786ms)
✔ getTaskTone returns 'success' for active/running/done states (0.1289ms)
✔ getTaskTone returns 'info' for review (0.1239ms)
✔ getTaskTone returns 'danger' for error/blocked/failed (0.0913ms)
✔ getTaskTone returns 'warning' for assigned (0.0913ms)
✔ getTaskTone returns 'neutral' for unknown states (0.1291ms)
✔ VALID_TRANSITIONS covers all 9 task states (0.1705ms)
✔ done and cancelled are terminal states (no outgoing transitions) (0.626ms)
✔ isValidTransition allows valid state changes (0.1354ms)
✔ isValidTransition rejects invalid state changes (0.0893ms)
✔ isValidTransition allows cancellation from most states (0.0878ms)
✔ isValidTransition returns false for unknown states (0.0745ms)
✔ isValidTransition allows failed -> assigned (reassignment) (0.0631ms)
ℹ tests 143
ℹ suites 0
ℹ pass 142
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 307.9727

✖ failing tests:

test at tests\office-preview-shell.test.mjs:54:1
✖ office preview shell gives Floor Overview a map plus inspector command center (4.0046ms)
  AssertionError [ERR_ASSERTION]: The input did not match the regular expression /floor-command-center/. Input:

  'import React, { useEffect, useMemo, useState } from "react";\n' +
    'import layoutStyles from "../office.module.css";\n' +
    'import { OfficeSidebar } from "./OfficeSidebar.js";\n' +
    'import {\n' +
    '  OFFICE_STAGE_MODE_STORAGE_KEY,\n' +
    '  OfficeStage,\n' +
    '  getInitialOfficeStageMode,\n' +
    '  type OfficeStageMode,\n' +
    '} from "./OfficeStage.js";\n' +
    'import { createOfficePresenceModel, resolveOfficeSelection } from "../office-presence-model.js";\n' +
    'import { compareOfficeTasks } from "../office-system.js";\n' +
    'import type { AgentState, EventRecord, TaskState } from "../store/event-store.js";\n' +
    '\n' +
    'export function PixelOffice({\n' +
    '  agents,\n' +
    '  tasks,\n' +
    '  events = [],\n' +
    '  onOpenAgent,\n' +
    '}: {\n' +
    '  agents: AgentState[];\n' +
    '  tasks: TaskState[];\n' +
    '  events?: EventRecord[];\n' +
    '  onOpenAgent?: (agentId: string) => void;\n' +
    '}) {\n' +
    '  const office = useMemo(\n' +
    '    () => createOfficePresenceModel({ agents, tasks, events }),\n' +
    '    [agents, tasks, events],\n' +
    '  );\n' +
    '  const [selectedRoomId, setSelectedRoomId] = useState<string | null>(() => {\n' +
    '    const storedRoomId = window.sessionStorage.getItem("conitens.officeFocusRoom");\n' +
    '    return office.rooms.some((room) => room.roomId === storedRoomId)\n' +
    '      ? storedRoomId\n' +
    '      : office.rooms[0]?.roomId ?? null;\n' +
    '  });\n' +
    '  const [selectedResidentId, setSelectedResidentId] = useState<string | null>(\n' +
    '    office.residents[0]?.agentId ?? null,\n' +
    '  );\n' +
    '  const [stageMode, setStageMode] = useState<OfficeStageMode>(getInitialOfficeStageMode);\n' +
    '\n' +
    '  useEffect(() => {\n' +
    '    const resolved = resolveOfficeSelection({\n' +
    '      rooms: office.rooms,\n' +
    '      selectedRoomId,\n' +
    '      selectedResidentId,\n' +
    '    });\n' +
    '    if (resolved.selectedRoomId !== selectedRoomId) {\n' +
    '      setSelectedRoomId(resolved.selectedRoomId);\n' +
    '    }\n' +
    '    if (resolved.selectedResidentId !== selectedResidentId) {\n' +
    '      setSelectedResidentId(resolved.selectedResidentId);\n' +
    '    }\n' +
    '  }, [office.rooms, selectedResidentId, selectedRoomId]);\n' +
    '\n' +
    '  useEffect(() => {\n' +
    '    window.sessionStorage.setItem(OFFICE_STAGE_MODE_STORAGE_KEY, stageMode);\n' +
    '  }, [stageMode]);\n' +
    '\n' +
    '  const selectedRoom =\n' +
    '    office.rooms.find((room) => room.roomId === selectedRoomId) ??\n' +
    '    office.rooms[0] ??\n' +
    '    null;\n' +
    '  const selectedResident =\n' +
    '    office.residents.find((resident) => resident.agentId === selectedResidentId) ?? null;\n' +
    '  const queuedTasks = useMemo(\n' +
    '    () =>\n' +
    '      tasks\n' +
    '        .slice()\n' +
    '        .sort((left, right) => compareOfficeTasks(left.state, right.state)),\n' +
    '    [tasks],\n' +
    '  );\n' +
    '  const liveRoomCount = useMemo(\n' +
    '    () => office.rooms.filter((room) => room.snapshot.runningCount > 0).length,\n' +
    '    [office.rooms],\n' +
    '  );\n' +
    '  const runningResidentCount = useMemo(\n' +
    '    () => office.residents.filter((resident) => resident.status === "running").length,\n' +
    '    [office.residents],\n' +
    '  );\n' +
    '  const blockedTaskCount = useMemo(\n' +
    '    () => queuedTasks.filter((task) => task.state === "blocked").length,\n' +
    '    [queuedTasks],\n' +
    '  );\n' +
    '  const selectedRoomTaskCount = selectedRoom?.snapshot.taskCount ?? 0;\n' +
    '  const selectedRoomRunningCount = selectedRoom?.snapshot.runningCount ?? 0;\n' +
    '  const selectedRoomHandoffCount = selectedRoom\n' +
    '    ? office.handoffs.filter((handoff) => {\n' +
    '        const actor = office.residents.find((resident) => resident.agentId === handoff.actorId);\n' +
    '        const target = handoff.targetId\n' +
    '          ? office.residents.find((resident) => resident.agentId === handoff.targetId)\n' +
    '          : null;\n' +
    '        return actor?.roomId === selectedRoom.roomId || target?.roomId === selectedRoom.roomId;\n' +
    '      }).length\n' +
    '    : 0;\n' +
    '  const surfacedTaskCount = queuedTasks.length;\n' +
    '  const blockedFocusTask = queuedTasks.find((task) => task.state === "blocked");\n' +
    '  const activeThreadLabel = selectedResident?.taskCount === 1 ? "thread" : "threads";\n' +
    '  const focusSummary = selectedResident\n' +
    '    ? `${selectedResident.agentId} is in ${selectedResident.roomLabel}, carrying ${selectedResident.taskCount} active ${activeThreadLabel}.`\n' +
    '    : selectedRoom\n' +
    '      ? `${selectedRoom.label} has ${selectedRoomTaskCount} surfaced tasks, ${selectedRoomRunningCount} running lanes, and ${selectedRoomHandoffCount} handoffs touching the room.`\n' +
    '      : "Select a room or resident to inspect the active operating cadence.";\n' +
    '  const roomReason = selectedRoom\n' +
    '    ? `${selectedRoom.label}: ${selectedRoomRunningCount} running, ${selectedRoomTaskCount} tasks, ${selectedRoomHandoffCount} handoffs.`\n' +
    '    : "No room selected.";\n' +
    '  const focusedReason = blockedFocusTask\n' +
    '    ? `${blockedFocusTask.taskId} is waiting at the owner gate.`\n' +
    '    : "No blocked owner gate.";\n' +
    '\n' +
    '  const handleSelectRoom = (roomId: string) => {\n' +
    '    const room = office.rooms.find((entry) => entry.roomId === roomId);\n' +
    '    setSelectedRoomId(roomId);\n' +
    '    setSelectedResidentId(room?.visibleResidents[0]?.agentId ?? null);\n' +
    '  };\n' +
    '\n' +
    '  const handleSelectResident = (agentId: string) => {\n' +
    '    const resident = office.residents.find((entry) => entry.agentId === agentId);\n' +
    '    setSelectedResidentId(agentId);\n' +
    '    setSelectedRoomId(resident?.roomId ?? selectedRoomId);\n' +
    '  };\n' +
    '\n' +
    '  return (\n' +
    '    <div\n' +
    '      className={layoutStyles["office-frame"]}\n' +
    '      data-office-preview-shell={stageMode === "focused" ? "workbench-dominant" : "viewport-dominant"}\n' +
    '      data-office-stage-mode={stageMode}\n' +
    '    >\n' +
    '      <section\n' +
    '        className={layoutStyles["office-summary-band"]}\n' +
    '        data-summary-mode={stageMode === "focused" ? "compact" : "full"}\n' +
    '      >\n' +
    '        <div className={layoutStyles["office-summary-copy"]}>\n' +
    '          <h2 className={layoutStyles["office-summary-title"]}>\n' +
    '            {stageMode === "focused" ? "Active handoff posture" : "Current floor posture"}\n' +
    '          </h2>\n' +
    '          {stageMode !== "focused" ? (\n' +
    '            <p className={layoutStyles["office-summary-text"]}>{focusSummary}</p>\n' +
    '          ) : null}\n' +
    '          <p className={layoutStyles["office-summary-reason"]}>\n' +
    '            {stageMode === "focused" ? focusedReason : roomReason}\n' +
    '          </p>\n' +
    '        </div>\n' +
    '        {stageMode === "focused" ? (\n' +
    '          <div className={layoutStyles["office-summary-mode-note"]}>\n' +
    '            <span className={layoutStyles["office-focus-label"]}>Focus</span>\n' +
    '            <strong>{selectedResident ? selectedResident.agentId : selectedRoom?.label ?? "No focus selected"}</strong>\n' +
    '            <span className={layoutStyles["office-focus-meta"]}>\n' +
    '              {selectedResident ? `${selectedResident.roomLabel} / ${selectedResident.status}` : "Room focus"}\n' +
    '            </span>\n' +
    '          </div>\n' +
    '        ) : (\n' +
    '          <div className={layoutStyles["office-summary-side"]}>\n' +
    '            <div className={layoutStyles["office-summary-grid"]}>\n' +
    '              <div className={layoutStyles["office-summary-item"]}>\n' +
    '                <span className={layoutStyles["office-summary-label"]}>Live Rooms</span>\n' +
    '                <strong>{liveRoomCount}</strong>\n' +
    '                <span className={layoutStyles["office-summary-note"]}>{office.rooms.length} total zones</span>\n' +
    '              </div>\n' +
    '              <div className={layoutStyles["office-summary-item"]}>\n' +
    '                <span className={layoutStyles["office-summary-label"]}>Blocked Lanes</span>\n' +
    '                <strong>{blockedTaskCount}</strong>\n' +
    '                <span className={layoutStyles["office-summary-note"]}>{runningResidentCount} running operators</span>\n' +
    '              </div>\n' +
    '              <div className={layoutStyles["office-summary-item"]}>\n' +
    '                <span className={layoutStyles["office-summary-label"]}>Handoffs</span>\n' +
    '                <strong>{office.handoffCount}</strong>\n' +
    '                <span className={layoutStyles["office-summary-note"]}>{surfacedTaskCount} surfaced tasks</span>\n' +
    '              </div>\n' +
    '            </div>\n' +
    '            <div className={layoutStyles["office-focus-line"]}>\n' +
    '              <span className={layoutStyles["office-focus-label"]}>Focus</span>\n' +
    '              <strong>{selectedResident ? selectedResident.agentId : selectedRoom?.label ?? "No focus selected"}</strong>\n' +
    '              <span className={layoutStyles["office-focus-meta"]}>\n' +
    '                {selectedResident ? `${selectedResident.roomLabel} / ${selectedResident.status}` : "Select a room to inspect load"}\n' +
    '              </span>\n' +
    '              {selectedResident && onOpenAgent ? (\n' +
    '                <button\n' +
    '                  className={layoutStyles["office-focus-link"]}\n' +
    '                  type="button"\n' +
    '                  onClick={() => onOpenAgent(selectedResident.agentId)}\n' +
    '                >\n' +
    '                  Open in Agents\n' +
    '                </button>\n' +
    '              ) : null}\n' +
    '            </div>\n' +
    '          </div>\n' +
    '        )}\n' +
    '      </section>\n' +
    '      <div className={layoutStyles["office-layout"]} data-stage-mode={stageMode}>\n' +
    '        {agents.length === 0 ? (\n' +
    '          <div className="empty-state animated">No agents online. Waiting for heartbeats...</div>\n' +
    '        ) : (\n' +
    '          <>\n' +
    '            <OfficeStage\n' +
    '              rooms={office.rooms}\n' +
    '              tasks={queuedTasks}\n' +
    '              handoffs={office.handoffs}\n' +
    '              events={events}\n' +
    '              stageMode={stageMode}\n' +
    '              selectedRoomId={selectedRoomId}\n' +
    '              selectedResidentId={selectedResidentId}\n' +
    '              onStageModeChange={setStageMode}\n' +
    '              onSelectRoom={handleSelectRoom}\n' +
    '              onSelectResident={handleSelectResident}\n' +
    '            />\n' +
    '            <OfficeSidebar\n' +
    '              mode={stageMode === "focused" ? "focused" : "full"}\n' +
    '              handoffs={office.handoffs}\n' +
    '              residents={office.residents}\n' +
    '              queuedTasks={queuedTasks}\n' +
    '              selectedRoom={selectedRoom}\n' +
    '              selectedResident={selectedResident}\n' +
    '              selectedResidentId={selectedResidentId}\n' +
    '              onSelectResident={handleSelectResident}\n' +
    '              onOpenAgent={onOpenAgent}\n' +
    '            />\n' +
    '          </>\n' +
    '        )}\n' +
    '      </div>\n' +
    '    </div>\n' +
    '  );\n' +
    '}\n'

      at TestContext.<anonymous> (file:///D:/Google/.Conitens/packages/dashboard/tests/office-preview-shell.test.mjs:60:10)
      at Test.runInAsyncScope (node:async_hooks:227:14)
      at Test.run (node:internal/test_runner/test:1306:25)
      at Test.processPendingSubtests (node:internal/test_runner/test:897:18)
      at Test.postRun (node:internal/test_runner/test:1447:19)
      at Test.run (node:internal/test_runner/test:1372:12)
      at async startSubtestAfterBootstrap (node:internal/test_runner/harness:385:3) {
    generatedMessage: true,
    code: 'ERR_ASSERTION',
    actual: 'import React, { useEffect, useMemo, useState } from "react";\nimport layoutStyles from "../office.module.css";\nimport { OfficeSidebar } from "./OfficeSidebar.js";\nimport {\n  OFFICE_STAGE_MODE_STORAGE_KEY,\n  OfficeStage,\n  getInitialOfficeStageMode,\n  type OfficeStageMode,\n} from "./OfficeStage.js";\nimport { createOfficePresenceModel, resolveOfficeSelection } from "../office-presence-model.js";\nimport { compareOfficeTasks } from "../office-system.js";\nimport type { AgentState, EventRecord, TaskState } from "../store/event-store.js";\n\nexport function PixelOffice({\n  agents,\n  tasks,\n  events = [],\n  onOpenAgent,\n}: {\n  agents: AgentState[];\n  tasks: TaskState[];\n  events?: EventRecord[];\n  onOpenAgent?: (agentId: string) => void;\n}) {\n  const office = useMemo(\n    () => createOfficePresenceModel({ agents, tasks, events }),\n    [agents, tasks, events],\n  );\n  const [selectedRoomId, setSelectedRoomId] = useState<string | null>(() => {\n    const storedRoomId = window.sessionStorage.getItem("conitens.officeFocusRoom");\n    return office.rooms.some((room) => room.roomId === storedRoomId)\n      ? storedRoomId\n      : office.rooms[0]?.roomId ?? null;\n  });\n  const [selectedResidentId, setSelectedResidentId] = useState<string | null>(\n    office.residents[0]?.agentId ?? null,\n  );\n  const [stageMode, setStageMode] = useState<OfficeStageMode>(getInitialOfficeStageMode);\n\n  useEffect(() => {\n    const resolved = resolveOfficeSelection({\n      rooms: office.rooms,\n      selectedRoomId,\n      selectedResidentId,\n    });\n    if (resolved.selectedRoomId !== selectedRoomId) {\n      setSelectedRoomId(resolved.selectedRoomId);\n    }\n    if (resolved.selectedResidentId !== selectedResidentId) {\n      setSelectedResidentId(resolved.selectedResidentId);\n    }\n  }, [office.rooms, selectedResidentId, selectedRoomId]);\n\n  useEffect(() => {\n    window.sessionStorage.setItem(OFFICE_STAGE_MODE_STORAGE_KEY, stageMode);\n  }, [stageMode]);\n\n  const selectedRoom =\n    office.rooms.find((room) => room.roomId === selectedRoomId) ??\n    office.rooms[0] ??\n    null;\n  const selectedResident =\n    office.residents.find((resident) => resident.agentId === selectedResidentId) ?? null;\n  const queuedTasks = useMemo(\n    () =>\n      tasks\n        .slice()\n        .sort((left, right) => compareOfficeTasks(left.state, right.state)),\n    [tasks],\n  );\n  const liveRoomCount = useMemo(\n    () => office.rooms.filter((room) => room.snapshot.runningCount > 0).length,\n    [office.rooms],\n  );\n  const runningResidentCount = useMemo(\n    () => office.residents.filter((resident) => resident.status === "running").length,\n    [office.residents],\n  );\n  const blockedTaskCount = useMemo(\n    () => queuedTasks.filter((task) => task.state === "blocked").length,\n    [queuedTasks],\n  );\n  const selectedRoomTaskCount = selectedRoom?.snapshot.taskCount ?? 0;\n  const selectedRoomRunningCount = selectedRoom?.snapshot.runningCount ?? 0;\n  const selectedRoomHandoffCount = selectedRoom\n    ? office.handoffs.filter((handoff) => {\n        const actor = office.residents.find((resident) => resident.agentId === handoff.actorId);\n        const target = handoff.targetId\n          ? office.residents.find((resident) => resident.agentId === handoff.targetId)\n          : null;\n        return actor?.roomId === selectedRoom.roomId || target?.roomId === selectedRoom.roomId;\n      }).length\n    : 0;\n  const surfacedTaskCount = queuedTasks.length;\n  const blockedFocusTask = queuedTasks.find((task) => task.state === "blocked");\n  const activeThreadLabel = selectedResident?.taskCount === 1 ? "thread" : "threads";\n  const focusSummary = selectedResident\n    ? `${selectedResident.agentId} is in ${selectedResident.roomLabel}, carrying ${selectedResident.taskCount} active ${activeThreadLabel}.`\n    : selectedRoom\n      ? `${selectedRoom.label} has ${selectedRoomTaskCount} surfaced tasks, ${selectedRoomRunningCount} running lanes, and ${selectedRoomHandoffCount} handoffs touching the room.`\n      : "Select a room or resident to inspect the active operating cadence.";\n  const roomReason = selectedRoom\n    ? `${selectedRoom.label}: ${selectedRoomRunningCount} running, ${selectedRoomTaskCount} tasks, ${selectedRoomHandoffCount} handoffs.`\n    : "No room selected.";\n  const focusedReason = blockedFocusTask\n    ? `${blockedFocusTask.taskId} is waiting at the owner gate.`\n    : "No blocked owner gate.";\n\n  const handleSelectRoom = (roomId: string) => {\n    const room = office.rooms.find((entry) => entry.roomId === roomId);\n    setSelectedRoomId(roomId);\n    setSelectedResidentId(room?.visibleResidents[0]?.agentId ?? null);\n  };\n\n  const handleSelectResident = (agentId: string) => {\n    const resident = office.residents.find((entry) => entry.agentId === agentId);\n    setSelectedResidentId(agentId);\n    setSelectedRoomId(resident?.roomId ?? selectedRoomId);\n  };\n\n  return (\n    <div\n      className={layoutStyles["office-frame"]}\n      data-office-preview-shell={stageMode === "focused" ? "workbench-dominant" : "viewport-dominant"}\n      data-office-stage-mode={stageMode}\n    >\n      <section\n        className={layoutStyles["office-summary-band"]}\n        data-summary-mode={stageMode === "focused" ? "compact" : "full"}\n      >\n        <div className={layoutStyles["office-summary-copy"]}>\n          <h2 className={layoutStyles["office-summary-title"]}>\n            {stageMode === "focused" ? "Active handoff posture" : "Current floor posture"}\n          </h2>\n          {stageMode !== "focused" ? (\n            <p className={layoutStyles["office-summary-text"]}>{focusSummary}</p>\n          ) : null}\n          <p className={layoutStyles["office-summary-reason"]}>\n            {stageMode === "focused" ? focusedReason : roomReason}\n          </p>\n        </div>\n        {stageMode === "focused" ? (\n          <div className={layoutStyles["office-summary-mode-note"]}>\n            <span className={layoutStyles["office-focus-label"]}>Focus</span>\n            <strong>{selectedResident ? selectedResident.agentId : selectedRoom?.label ?? "No focus selected"}</strong>\n            <span className={layoutStyles["office-focus-meta"]}>\n              {selectedResident ? `${selectedResident.roomLabel} / ${selectedResident.status}` : "Room focus"}\n            </span>\n          </div>\n        ) : (\n          <div className={layoutStyles["office-summary-side"]}>\n            <div className={layoutStyles["office-summary-grid"]}>\n              <div className={layoutStyles["office-summary-item"]}>\n                <span className={layoutStyles["office-summary-label"]}>Live Rooms</span>\n                <strong>{liveRoomCount}</strong>\n                <span className={layoutStyles["office-summary-note"]}>{office.rooms.length} total zones</span>\n              </div>\n              <div className={layoutStyles["office-summary-item"]}>\n                <span className={layoutStyles["office-summary-label"]}>Blocked Lanes</span>\n                <strong>{blockedTaskCount}</strong>\n                <span className={layoutStyles["office-summary-note"]}>{runningResidentCount} running operators</span>\n              </div>\n              <div className={layoutStyles["office-summary-item"]}>\n                <span className={layoutStyles["office-summary-label"]}>Handoffs</span>\n                <strong>{office.handoffCount}</strong>\n                <span className={layoutStyles["office-summary-note"]}>{surfacedTaskCount} surfaced tasks</span>\n              </div>\n            </div>\n            <div className={layoutStyles["office-focus-line"]}>\n              <span className={layoutStyles["office-focus-label"]}>Focus</span>\n              <strong>{selectedResident ? selectedResident.agentId : selectedRoom?.label ?? "No focus selected"}</strong>\n              <span className={layoutStyles["office-focus-meta"]}>\n                {selectedResident ? `${selectedResident.roomLabel} / ${selectedResident.status}` : "Select a room to inspect load"}\n              </span>\n              {selectedResident && onOpenAgent ? (\n                <button\n                  className={layoutStyles["office-focus-link"]}\n                  type="button"\n                  onClick={() => onOpenAgent(selectedResident.agentId)}\n                >\n                  Open in Agents\n                </button>\n              ) : null}\n            </div>\n          </div>\n        )}\n      </section>\n      <div className={layoutStyles["office-layout"]} data-stage-mode={stageMode}>\n        {agents.length === 0 ? (\n          <div className="empty-state animated">No agents online. Waiting for heartbeats...</div>\n        ) : (\n          <>\n            <OfficeStage\n              rooms={office.rooms}\n              tasks={queuedTasks}\n              handoffs={office.handoffs}\n              events={events}\n              stageMode={stageMode}\n              selectedRoomId={selectedRoomId}\n              selectedResidentId={selectedResidentId}\n              onStageModeChange={setStageMode}\n              onSelectRoom={handleSelectRoom}\n              onSelectResident={handleSelectResident}\n            />\n            <OfficeSidebar\n              mode={stageMode === "focused" ? "focused" : "full"}\n              handoffs={office.handoffs}\n              residents={office.residents}\n              queuedTasks={queuedTasks}\n              selectedRoom={selectedRoom}\n              selectedResident={selectedResident}\n              selectedResidentId={selectedResidentId}\n              onSelectResident={handleSelectResident}\n              onOpenAgent={onOpenAgent}\n            />\n          </>\n        )}\n      </div>\n    </div>\n  );\n}\n',
    expected: /floor-command-center/,
    operator: 'match',
    diff: 'simple'
  }
D:\Google\.Conitens\packages\dashboard:
 ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL  @conitens/dashboard@0.1.0 test: `node --experimental-strip-types --test tests/*.test.mjs "office-preview-shell.test.mjs"`
Exit status 1
